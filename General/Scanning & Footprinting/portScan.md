# Port Scanning

**NMAP PORTS:**
```
$ nmap -Pn (ip)

$ nmap -Pn -sCV -0 -v ()

$ nmap -iL ips

$ nmap -iL ips -sV -0
```



> Agressive scan: nmap -A

> output normal: -oN test.txt

> excel format: -oX test.xml